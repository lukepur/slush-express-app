{
    "name": "<%= name %>",
    "description": "<%= description %>",
    "repository": {
        "type": "git",
        "url": "https://github.com/lukepur/<%= name %>.git"
    },
    "licenses": [{
        "type": "<$= license %>"
    }],
    "main": "src/index.js",
    "engines": {
        "node": ">= 0.10.26",
        "npm": ">=1.4.3"
    },
    "scripts": {
        "start": "node app.js",
        "test": "echo \"No tests\""
    },
    "dependencies": {
        <% if (includeFrontend) { %>"hbs": "^3.0.1",<% } %>
        <% if (includeMongo) { %>"mongoose": "^4.0.1",<% } %>
        "express": "~4.0.0",
        "dotenv": "~1.0.0"
    },
    "devDependencies": {
        <% if (includeFrontend) {%>"gulp": "^3.8.11",
        "browserify": "^10.1.3",
        "del": "^1.1.1",
        "vinyl-source-stream": "^1.1.0",
        "gulp-sass": "^2.0.0"<% } %>
    }
}
